Procesamiento de compras. Al ejecutar una compra valida al usuario (via usuario-service) y el juego (via juego-service), registra la transacción y permite consultar el historial de compras por usuario o por ID.

1.1.4 version
